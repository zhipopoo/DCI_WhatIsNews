version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: whatisnew-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-whatisnew}
      POSTGRES_USER: ${POSTGRES_USER:-whatisnew}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-whatisnew123}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-whatisnew} -d ${POSTGRES_DB:-whatisnew}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - whatisnew-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: whatisnew-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-whatisnew}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-whatisnew}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-whatisnew123}
      JWT_SECRET: ${JWT_SECRET:-WhatIsNew-JWT-Secret-Key-2024-Production}
      JWT_EXPIRATION: ${JWT_EXPIRATION:-86400000}
      # CORS_ALLOWED_ORIGIN_PATTERNS: ${CORS_ALLOWED_ORIGIN_PATTERNS:-*}
      FILE_UPLOAD_DIR: ${FILE_UPLOAD_DIR:-/app/uploads}
      ADMIN_DEFAULT_USERNAME: ${ADMIN_DEFAULT_USERNAME:-admin}
      ADMIN_DEFAULT_PASSWORD: ${ADMIN_DEFAULT_PASSWORD:-admin123}
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    volumes:
      - uploads_data:/app/uploads
    networks:
      - whatisnew-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: whatisnew-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    networks:
      - whatisnew-network

volumes:
  postgres_data:
    driver: local
  uploads_data:
    driver: local

networks:
  whatisnew-network:
    driver: bridge
